services:
  - type: web
    name: llm-nav-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERVE_STATIC
        value: "false"
      - key: PORT
        fromParam: PORT
      - key: DATABASE_URL
        value: /opt/render/project/data/llm-nav.db
      - key: ADMIN_PASSWORD
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false
    disk:
      name: llm-nav-data
      mountPath: /opt/render/project/data
      sizeGB: 1
