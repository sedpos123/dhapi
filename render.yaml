services:
  - type: web
    name: llm-nav-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERVE_STATIC
        value: "false"
      - key: DATABASE_URL
        value: /opt/render/project/data/llm-nav.db
      - key: ADMIN_PASSWORD
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://dhapi.pages.dev
      - key: PORT
        value: "10000"
    disk:
      name: llm-nav-data
      mountPath: /opt/render/project/data
      sizeGB: 1
